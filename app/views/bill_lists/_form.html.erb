<%= form_with(model: bill_list, local: true) do |form| %>
  <% if bill_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bill_list.errors.count, "error") %> prohibited this bill_list from being saved:</h2>

      <ul>
      <% bill_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
